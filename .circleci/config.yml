version: 2
jobs:
  build:
    docker:
      - image: circleci/node:10.15.0
    working_directory: ~/repo
    steps:
      - checkout
      - run:
          name: npm-install
          command: npm install
      - run:
          name: test
          command: 'npm test'